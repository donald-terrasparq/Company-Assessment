# Always install devDependencies, even when NODE_ENV=production (Render sets it
# for both services). The build needs typescript/tailwind at compile time and the
# worker runs via tsx. Project .npmrc beats npm's NODE_ENV-derived default, so
# this holds for any build command a service happens to carry.
include=dev
