import { Placeholder } from "@/components/shell/placeholder";

export default function ListsPage() {
  return (
    <Placeholder
      title="Lists"
      phase="built in Phase 2"
      description="Every uploaded list with its date-stamped name, company count against the 100-company cap, last run, tier breakdown, cost, and actions."
    />
  );
}
