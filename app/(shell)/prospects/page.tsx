import { Placeholder } from "@/components/shell/placeholder";

export default function ProspectsPage() {
  return (
    <Placeholder
      title="Prospects"
      phase="built in Phase 4"
      description="The ranked results table: list selector with VIEW ALL, tier distribution bar, filter chips, and the score-anatomy bar on every row. No lists yet — upload one to get started."
    />
  );
}
