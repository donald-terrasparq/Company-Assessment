import { Placeholder } from "@/components/shell/placeholder";

export default function SignalsPage() {
  return (
    <Placeholder
      title="Signals"
      phase="built in Phase 5"
      description="The signal library: plain-English descriptions, strength sliders, category chips, and the global fit / recency / confidence / tier controls with a live impact preview."
    />
  );
}
